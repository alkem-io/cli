query spacesChallengesOpportunitiesIds {
  spaces(filter: { visibilities: [ARCHIVED, ACTIVE, DEMO] }) {
    id
    nameID
    subspaces {
      id
      nameID
      subspaces {
        id
        nameID
      }
    }
  }
}
