query spaceSubspacesCommunities($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      nameID
      community {
        id
      }
      subspaces {
        id
        nameID
        community {
          id
        }
      }
    }
  }
}
